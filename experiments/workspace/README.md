# V6 Cognitive / Profession Ablation Workspace

This directory is the experiment-only Workspace root. The experiment grants
`filesystem.read` only. Tasks are self-contained, so normal runs should not
need any capability call; the readable workspace exists to exercise the real
Assignment and Admission boundaries without exposing the project tree.
